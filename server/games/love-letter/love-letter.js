import readline from 'readline';

class LoveLetter {
    constructor() {
        var random;
        var rando;
        var iOStream;
        //believe this will get replaced with api response just place holder
        const iOStream = readline.createInterface(
            {
                input: process.stdin,
                output: process.stdout
            }
        )
    }
    // Constructs a LoveLetter game.
    // @param ioStream  IMPLEMENT 
    LoveLetter(ioStream) {
        this.random = Math.random();
        this.iOStream = ioStream;
        var rando = //new random agnet
    }

    loveLetter() {
        this.LoveLetter(0, 0) //system.out should be second
        this.ps = 0; //system.out
    }

    playGame(agents) {
        let gameOver = false;
        let winner = 0;
        let numPlayers = agents.length;
        // create new game state instance
        // create new player state instance 
        try {
            while ()//not gameover
                for (let i = 0; i < numPlayers; i++) {

                }
        }
    }
}

const server = new LoveLetter();
server.playGame();